return {
  "akinsho/bufferline.nvim",
  -- opts will be merged with the parent spec
  opts = {
    options = {
      padding = {
        left = 2, -- Increase the padding on the left
        right = 2, -- Increase the padding on the right
      },
    },
  },
}
